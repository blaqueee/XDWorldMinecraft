const bbox = [-180.0, -90.0, 180.0, 90.0];
const maxHeight = 20;
const blockSizeMeters = 10;
const blockHalfSize = blockSizeMeters / 2;
const offsetY = 0.0;

const seed = -860899055;
const chunkSize = 4;
const renderDistance = 2;

const metersPerLatDegree = 111_320;